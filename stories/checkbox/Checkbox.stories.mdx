import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs';
import {Checkbox, meta, Buttons, Sizes} from './Checkbox.story';

# Checkbox

<Meta title={meta.title} component={meta.component} argTypes={meta.argTypes} args={meta.args} />

## Button

A new `<Checkbox.Button />` component is available to bring button styles to checkboxes

<Canvas>
  <Story name="Button">{Buttons}</Story>
</Canvas>

<ArgsTable of={Checkbox.Group} story="^" include={['size', 'layout']} />

## Sizes

The `<Checkbox.Group />` component does support a new `size` prop that matches the `SizeProps` of the extended `<Button />` component.

<Canvas>
  <Story name="Sizes">{Sizes}</Story>
</Canvas>

## Props

<ArgsTable of={Checkbox.Group} />
